#Beatport Verifier

Dependencies: python2.7, BeautifulSoup4.
Current State: Not Working.*

*Beatport markup seems to have changed since last use (around year 2012)

Once run, the program looks if Data/tracks.txt is present. then it fetches
top-100 tracks from beatport and determines new entries. Latter are 
written to Data/newtracks.txt
